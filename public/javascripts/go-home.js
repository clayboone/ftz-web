/**
 * Wait half a second, then zoom logo into view.
 */
setTimeout(function () {

}, 500);

/**
 * Then go to real home page.
 */
setTimeout(function() {
    window.open('/home', '_self');
}, 2000);
