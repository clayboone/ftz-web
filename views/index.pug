extends layout

block content
  h1.
    Memory used: <em class="used-mem">#{memUsed}/#{memTotal}</em>
  //- progress(max=#{memTotal}, value=#{memUsed})

  h2 This is a change!
  h2 This is a another change! again
